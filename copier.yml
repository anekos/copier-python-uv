project_name:
  type: str
  help: What is your project name?

package_name:
  type: str
  default: "{{ project_name.lower().replace(' ', '_') }}"

project_type:
  type: str
  help: プロジェクトの種類を選んでください
  choices:
    - local
    - lambda_sam
    - lambda_terraform
